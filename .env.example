# Copy to .env and fill in the keys you need:  cp .env.example .env
# .env is gitignored and will never be committed.
# Verify with:  python common/env_keys.py

# OpenAI — Moderation API, COCO prompt generation, dialect translation
OPENAI_API_KEY=

# Anthropic — back-translation LLM judge
ANTHROPIC_API_KEY=

# Google Gemini — back-translation
GEMINI_API_KEY=

# HuggingFace Hub — gated models only; leave blank if you only use public models
HF_TOKEN=
